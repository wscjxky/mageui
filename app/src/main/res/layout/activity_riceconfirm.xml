<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

<android.support.v7.widget.Toolbar
    android:id="@+id/toolbar"
    style="@style/mytoolbar">

</android.support.v7.widget.Toolbar>
    <LinearLayout
        android:padding="15dp"
        android:id="@+id/riceconfirm_linel_address"
        style="@style/wrap_layout"
        android:orientation="horizontal"
        >

        <TextView
            style="@style/usertext"
            android:text="收货地址"
            />

        <ImageView style="@style/img_arrow"
            />
    </LinearLayout>
    <LinearLayout
        android:padding="15dp"
        android:id="@+id/riceconfirm_linel_time"
        style="@style/wrap_layout"
        android:orientation="horizontal"
        >
        <TextView
            android:id="@+id/textView9"
            style="@style/usertext"
            android:text="预计送达时间"
            />

        <TextView
            style="@style/usertext"
            android:gravity="end"
            android:text="14:00:00"
            />
    </LinearLayout>
    <LinearLayout
        android:padding="10dp"
        android:id="@+id/riceconfirm_linel_menu"
        style="@style/wrap_layout"
        android:orientation="vertical"
        >
        <TextView
            style="@style/usertext"
            android:layout_gravity="center"
            android:text="菜单"
            />
        <ListView
            android:padding="10dp"
            android:id="@+id/riceconfirm_listview"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            >
        </ListView>

    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <Button
            android:id="@+id/riceconfirm_btn_submit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/yellow"

            android:text="提交订单  ￥" />
    </LinearLayout>

</LinearLayout>